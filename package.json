{
  "name": "mcp-email-aws-ses-server",
  "version": "1.1.0",
  "description": "MCP server for AWS SES email integration",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.78.0",
    "vitest": "^1.2.0"
  }
}